# دليل مناقشة مشروع كشف الصور المزيفة (Fake Image Detector)

هذا الدليل مصمم خصيصاً لمساعدتك في الإجابة على أسئلة لجنة المناقشة (الدكتور) بالتفصيل والأسلوب العلمي الصحيح.

---

## أولاً: الإجابة على سؤال "استخدمت إيه في الربط وربطت إزاي؟"

يركز هذا السؤال على **معمارية النظام (System Architecture)** وكيفية اتصال واجهة المستخدم (Frontend) بالخلفية البرمجية (Backend) ونموذج الذكاء الاصطناعي (AI Model).

### 1. التقنيات المستخدمة في الربط (Tech Stack):
*   **الخلفية البرمجية (Backend API):** تم استخدام إطار عمل **Flask** (وهو إطار عمل خفيف وسريع بلغة بايثون Python).
*   **الواجهة الأمامية (Frontend):** تم استخدام تقنيات الويب الأساسية: **HTML5** للهيكلة، **CSS3** للتصميم العصري والحركات، و **Vanilla JavaScript (ES6+)** لمنطق التفاعل والربط.
*   **بروتوكول الاتصال:** تم الربط عبر بروتوكول **HTTP** باستخدام طلبات غير متزامنة (**Asynchronous HTTP POST Request**).
*   **تنسيق البيانات (Data Format):** يتم إرسال الصورة كـ **FormData**، ويقوم الخادم بالرد على الواجهة بتنسيق **JSON** (JavaScript Object Notation).

### 2. كيف تم الربط بالتفصيل (طريقة العمل خطوة بخطوة):
1.  **رفع الصورة:** يقوم المستخدم برفع الصورة في الواجهة (عن طريق السحب والإفلات أو الاختيار).
2.  **طلب التحليل (Request):** عند الضغط على زر **Run Analysis**، يقوم كود الجافا سكريبت في ملف `main.js` بإنشاء كائن من نوع `FormData` وإضافة ملف الصورة إليه.
3.  **إرسال الطلب (Fetch API):** يتم إرسال الصورة عبر طلب `POST` إلى نقطة النهاية (Endpoint) الخاصة بالخادم وهي `/predict` (تحديداً على الرابط `http://127.0.0.1:5000/predict`).
4.  **المعالجة المسبقة (Preprocessing):** يستقبل خادم Flask في ملف `app.py` الصورة، ويقوم بتحويلها إلى نظام الألوان RGB، ثم يطبق عليها عمليات معالجة مسبقة متطابقة تماماً مع مرحلة تدريب الموديل:
    *   تغيير الأبعاد إلى **112×112 بكسل** (وهو الحجم الذي تدرب عليه الموديل).
    *   تحويل الصورة إلى مصفوفة رقمية (Tensor) يفهمها مكتبة PyTorch.
5.  **التنبؤ (Inference):** يتم تمرير الصورة المجهزة إلى نموذج الشبكة العصبية **ResNet-50** المحمل في الذاكرة. يقوم النموذج بحساب الاحتمالية ويخرج قيمة تقع بين 0 و 1 (بسبب استخدام دالة التنشيط Sigmoid في الطبقة الأخيرة).
6.  **إرجاع النتيجة (Response):** يقوم السيرفر بتحويل النتيجة رياضياً:
    *   إذا كان الناتج **أكبر من 0.5**، تُصنف الصورة على أنها **حقيقية (Real)**، ونسبة الثقة هي القيمة نفسها (مثلاً 0.82 تعني حقيقية بنسبة 82%).
    *   إذا كان الناتج **أقل من أو يساوي 0.5**، تُصنف الصورة على أنها **مزيفة (Fake)**، ونسبة الثقة هي متمم القيمة للواحد الصحيح (مثلاً إذا كان الناتج 0.15، تكون مزيفة بنسبة ثقة 85%).
    *   يتم إرسال هذه البيانات (Prediction و Confidence و Filename) كـ **JSON response** إلى المتصفح.
7.  **تحديث الواجهة (UI Rendering):** تستقبل الجافا سكريبت رد السيرفر، وتوقف حركة التحميل، وتُظهر لوحة النتائج (اللون الأحمر والتحذير إذا كانت Fake، أو الأخضر وعلامة التحقق إذا كانت Real)، كما تقوم برسم الرسم البياني الراداري (Radar Chart) وتحديث شريط نسبة الثقة.

---

## ثانياً: شرح وظيفة كل ملف في المشروع (File Structure)

إليك ملفات المشروع ودور كل منها بالتفصيل:

### 1. `app.py` (خادم الخلفية والذكاء الاصطناعي - Backend Server)
*   **دوره:** هو القلب النابض للمشروع من ناحية البرمجة الخلفية.
*   **الوظائف الرئيسية:**
    *   تشغيل سيرفر الويب المحلي (Local Flask Server) على المنفذ (Port 5000).
    *   تعريف معمارية نموذج **ResNet-50** وإضافة طبقات التصنيف المخصصة (Fully Connected Layers) التي تم استخدامها أثناء التدريب.
    *   تحميل أوزان الموديل المدرب من ملف `deepfake_model_150px.pth`.
    *   استقبال طلبات رفع الصور على مسار `/predict` والتحقق من امتداد الصورة وحجمها كإجراء أمني.
    *   القيام بالمعالجة المسبقة للصور وتمريرها للموديل للحصول على التوقع وإرجاعه للواجهة كملف JSON.
    *   تفعيل ميزة **CORS** (Cross-Origin Resource Sharing) للسماح للواجهة الأمامية بالاتصال بالخلفية بدون قيود أمنية أثناء التطور المحلي.

### 2. `index.html` (واجهة المستخدم - Frontend UI)
*   **دوره:** يمثل الهيكل البنائي لصفحة الويب التي يراها المستخدم.
*   **الوظائف الرئيسية:**
    *   يحتوي على شريط التنقل (Navigation Bar).
    *   يضم منطقة السحب والإفلات المخصصة لرفع الصور.
    *   يحتوي على شاشة التحميل التفاعلية التي تظهر أثناء معالجة الموديل للصورة.
    *   يحتوي على لوحة عرض النتائج (Dashboard) التي تعرض النسبة المئوية للثقة، والرسوم البيانية، والبيانات الميتا (Metadata) الخاصة بالصورة، والمؤشرات الجنائية (Forensic Indicators).
    *   يربط المشروع بملفات التصميم الخارجي `style.css` وملف البرمجة التفاعلي `main.js`.

### 3. `main.js` (التحكم والتفاعل والربط - Frontend Controller)
*   **دوره:** هو العقل المفكر للواجهة الأمامية والمسؤول عن ربطها مع الـ API.
*   **الوظائف الرئيسية:**
    *   التحكم في عمليات السحب والإفلات (Drag and Drop) وقراءة الصور محلياً لعرضها كمعاينة قبل التحليل.
    *   تشغيل وإدارة الحركات التفاعلية أثناء شاشة التحميل (Simulation steps: Feature Extraction, Freq Analysis, etc.).
    *   إرسال طلب الـ `fetch` غير المتزامن إلى سيرفر Flask حاملًا الصورة.
    *   استقبل رد السيرفر وفك تشفير كائن الـ JSON.
    *   تعديل عناصر صفحة HTML ديناميكياً بناءً على النتيجة (عرض Verdict، وتغيير الألوان والنصوص بناءً على ما إذا كانت الصورة حقيقية أم مزيفة).
    *   استخراج وعرض بيانات الصورة الوصفية (Metadata) مثل الأبعاد والامتداد والحجم.
    *   رسم **الرسم البياني الراداري (Radar Chart)** باستخدام عنصر HTML5 `<canvas>` لعرض الأبعاد التحليلية للصورة (AI-Gen, Authentic, Tampered, Freq, Noise, ELA).

### 4. `style.css` (التصميم والمظهر - UI Styling)
*   **دوره:** يعطي الموقع مظهره العصري والاحترافي.
*   **الوظائف الرئيسية:**
    *   تطبيق الهوية البصرية المظلمة (Premium Dark Mode) باستخدام تدرجات الألوان المتناسقة (مثل درجات الأزرق، البنفسجي، الأحمر الفاقع للتحذير، والأخضر للتأكيد).
    *   تصميم تخطيط الصفحة بشكل مرن ومتجاوب مع جميع الشاشات (Responsive Grid & Flexbox).
    *   تنسيق الأزرار، والبطاقات، ومربعات الرفع، وشاشات التحميل.
    *   إضافة الحركات الدقيقة التفاعلية (Micro-animations) مثل تأثير نبض حلقة الرفع وتأثير حركة خط الفحص الليزري (Scanning Line).

### 5. `deepfake_model_150px.pth` (أو `model.pth`) (أوزان النموذج - Model Weights)
*   **دوره:** هو ملف ثنائي (Binary File) يحتوي على معلمات وأوزان الشبكة العصبية (Neural Network Weights) التي تم الوصول إليها بعد عملية التدريب الطويلة على مجموعة البيانات (Dataset). يقوم ملف `app.py` بتحميله ليصبح الموديل قادراً على التعرف على الصور الحقيقية والمزيفة فوراً دون الحاجة لإعادة تدريب.

### 6. `Yet_another_deepfakeimagedetectionresnet50.ipynb` (دفتر العمل - Training Notebook)
*   **دوره:** هو ملف Jupyter Notebook يحتوي على الكود الكامل لعملية بناء الموديل وتدريبه واختباره. يشمل استيراد البيانات، وتقسيمها، وضبط المعاملات الفائقة (Hyperparameters)، ومراحل تدريب نموذج ResNet-50 وحفظ الأوزان النهائية في ملف `.pth`.

### 7. `get_transforms.py`
*   **دوره:** كود برمجي صغير مساعد (Helper script) مكتوب بلغة بايثون للبحث داخل ملف الـ Jupyter Notebook المذكور أعلاه واستخراج الأسطر البرمجية الخاصة بالتحويلات البرمجية للصور (Transforms) وحفظها في ملف نصي للتأكد من مطابقة المعالجة في السيرفر مع التدريب.

### 8. `out.txt`
*   **دوره:** ملف نصي ناتج عن تشغيل ملف `get_transforms.py` يحتوي على أسطر الكود المستخرجة الخاصة بالتحويلات لمراجعتها وتطبيقها في السيرفر.

### 9. `img00.jpeg`
*   **دوره:** صورة توضيحية تستخدم كخلفية أو عنصر مرئي في الواجهة الأمامية (Hero Section) للموقع لإعطاء شكل جمالي للمشروع.

---

## ثالثاً: أهم الأسئلة المتوقعة في المناقشة وإجاباتها النموذجية

1.  **س: لماذا اخترتم نموذج ResNet-50 تحديداً؟**
    *   **ج:** لأن معمارية ResNet (Residual Networks) قوية جداً في حل مشكلة "تلاشي الانحدار" بفضل الـ **Skip Connections**. كما أن حجم النموذج (50 طبقة) مناسب جداً للحصول على دقة عالية في تصنيف عيوب البكسلات والأنماط الدقيقة التي تتركها مولدات الذكاء الاصطناعي، وفي نفس الوقت لا يستهلك موارد معالجة ضخمة جداً مثل الموديلات الأكبر.
2.  **س: ما هي دالة التنشيط (Activation Function) المستخدمة في الطبقة الأخيرة ولماذا؟**
    *   **ج:** دالة التنشيط هي **Sigmoid** لأنها تقوم بتحويل ناتج الموديل النهائي إلى قيمة احتمالية محصورة تماماً بين **0 و 1**، وهو ما يسهل علينا تصنيف الصورة ثنائياً (Binary Classification: إما حقيقية أو مزيفة) وتحديد نسبة الثقة.
3.  **س: كيف يتعامل النظام مع الصور ذات الأحجام المختلفة المرفوعة؟**
    *   **ج:** يتم تمرير أي صورة مرفوعة على دالة التحويل `transforms.Resize((112, 112))` في بايثون قبل تمريرها للموديل. هذا يضمن أن تدخل الصورة للموديل بحجم ثابت 112×112 بكسل بغض النظر عن حجمها الأصلي، وهو الحجم المحدد في طبقة الإدخال الخاصة بالشبكة العصبية.
4.  **س: هل السيرفر يعمل محلياً أم مرفوع على الإنترنت؟**
    *   **ج:** السيرفر يعمل محلياً (Local Host) على جهاز الكمبيوتر باستخدام Flask على العنوان `127.0.0.1:5000` وهو كافٍ تماماً لغرض المناقشة والعرض المباشر.
